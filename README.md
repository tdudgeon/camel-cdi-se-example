# Camel CDI Java SE example using Gradle

Simple minimal Gradle build file for a Java SE based Apache Camel project with CDI (using Weld)
To run the project:

```sh
./gradlew installDist
build/install/cdi_example/bin/cdi_example
``` 
